import { CommonAttributesService } from './common-attributes.service';

describe('CommonAttributesService', () => {
  it('create an instance', () => {
    const service = new CommonAttributesService();
    expect(service).toBeTruthy();
  });
});
